pipeline {
    agent any
    
    
    
    stages {
        /*
        stage('Git Checkout') {
            steps {
                git branch: 'dev', url: 'https://github.com/jaiswaladi246/3-Tier-DevSecOps-Mega-Project.git'
            }
        }*/
        
        stage('Frontend Compilation') {
            steps {

 
     dir('client') {

            withCredentials([aws(accessKeyVariable: 'AWS_ACCESS_KEY_ID', credentialsId: 'ECR_442042548826', secretKeyVariable: 'AWS_SECRET_ACCESS_KEY')]) {
                sh 'echo ======================'
                sh 'echo ${AWS_ACCESS_KEY_ID}'
                sh 'docker login -u AWS -p $(aws ecr get-login-password --region us-east-1) 442042548826.dkr.ecr.us-east-1.amazonaws.com'
     sh 'docker build -t 442042548826.dkr.ecr.us-east-1.amazonaws.com/anandhabadmanaban/3_tier_app_frontend:{ BUILD_COMMIT } .'
     sh 'docker push 442042548826.dkr.ecr.us-east-1.amazonaws.com/anandhabadmanaban/3_tier_app_frontend:{ BUILD_COMMIT }'
}
                   
                    
                }
}

               
            }


        
        
        stage('Backend Compilation') {
            steps {
                dir('api') {
                     withCredentials([aws(accessKeyVariable: 'AWS_ACCESS_KEY_ID', credentialsId: 'ECR_442042548826', secretKeyVariable: 'AWS_SECRET_ACCESS_KEY')]) {
                       sh 'aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin 442042548826.dkr.ecr.us-east-1.amazonaws.com'
                   sh 'docker build -t 442042548826.dkr.ecr.us-east-1.amazonaws.com/anandhabadmanaban/3_tier_app_backend:{ BUILD_COMMIT } .'
                   sh 'docker push 442042548826.dkr.ecr.us-east-1.amazonaws.com/anandhabadmanaban/3_tier_app_backend:{ BUILD_COMMIT }'
                     }
                }
            }
        }
         /*
        stage('GitLeaks Scan') {
            steps {
                sh 'gitleaks detect --source ./client --exit-code 1'
                sh 'gitleaks detect --source ./api --exit-code 1'
            }
        }
       
        stage('SonarQube Analysis') {
            steps {
                withSonarQubeEnv('sonar') {
                    sh ''' $SCANNER_HOME/bin/sonar-scanner -Dsonar.projectName=NodeJS-Project \
                            -Dsonar.projectKey=NodeJS-Project '''
                }
            }
        }
        stage('Quality Gate Check') {
            steps {
                timeout(time: 1, unit: 'HOURS') {
                    waitForQualityGate abortPipeline: false, credentialsId: 'sonar-token'
                }
            }
        }
        stage('Trivy FS Scan') {
            steps {
                sh 'trivy fs --format table -o fs-report.html .'
            }
        }*/
    }
}

 

